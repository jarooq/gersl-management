<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext (HTTP) traffic is denied by default. It is permitted ONLY for the
  local development hosts the app talks to during `flutter run`:
    * 10.0.2.2  — the Android emulator's alias for the host machine
    * localhost — used when port-forwarded / on-device debugging
  Production traffic (Vercel) is HTTPS and is unaffected by this config.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
